# Contributing

이 예제의 목표는 별도 DB 설치 없이 재현 가능한 멀티 데이터소스 학습 환경을
제공하는 것입니다. 변경 사항도 이 목표를 유지해야 합니다.

## 개발 환경

- JDK 21을 사용합니다.
- 시스템 Gradle 대신 저장소의 `./gradlew` 또는 `gradlew.bat`를 사용합니다.
- H2와 SQLite 테스트 파일은 OS 임시 디렉터리에 생성합니다.
- 외부 DB 자격 증명이나 개발자 PC의 절대 경로를 추가하지 않습니다.

## 변경 절차

1. 동작 변경이나 버그 수정은 먼저 실패하는 테스트로 계약을 정의합니다.
2. 두 데이터소스와 트랜잭션 매니저의 역할을 명시적으로 유지합니다.
3. 런타임 동작이나 명령이 바뀌면 README의 실행 예제도 함께 갱신합니다.
4. Pull Request를 열기 전에 다음 명령을 실행합니다.

```bash
./gradlew clean test --no-daemon
git diff --check
```

테스트가 프로젝트의 `./data/`를 생성하거나 변경하면 안 됩니다. 테스트
설정은 `BatchIntegrationTestSupport`의 임시 파일 경로를 사용해야 합니다.
